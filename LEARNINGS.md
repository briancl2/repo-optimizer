# LEARNINGS — repo-optimizer

| # | Learning | Source |
|---|---|---|
| L1 | repo-optimizer was a script directory with 0 self-authored commits, dormant spec-kit, and no self-management infrastructure. Every repo-agent needs a build spec — the spec IS the repo-agent's DNA. | spec 054 RCA, L272 |
| L2 | Agent must call repo-optimizer.sh directly as a single command, not dispatch discovery subagents individually. The bash orchestrator manages directory layout and phase sequencing that downstream tools depend on. | spec 054, L274/L6 |
| L3 | First self-managed work contract (M5.5 validation) confirmed the full work-init -> edit -> work-close -> SCORECARD cycle works end-to-end. Session grader produces valid OPERATING_MODEL_SCORECARD.json. Domain tests are unaffected by self-management infrastructure. | spec 054 M5.5 validation, v94 |
| L4 | Line-continuation backslashes in multi-line find commands are invisible failure points: the minimal budget tier find on line 116 of repo-optimizer.sh was missing a trailing backslash after the opening parenthesis grouping operator, causing "find: (): empty inner expression" on any repo with >1000 files. The focused tier (line 95) had the correct syntax. Fix was 1 character. Regression test with synthetic repos at all 3 tier boundaries (50, 500, 1100 files) now prevents recurrence. | L270 (BMA), v96 work contract |
| L5 | Fleet-generated spec cycle proof: DS-20 drift was caused by spec-kit infrastructure added in spec 054 not being documented. Root cause: make check validates inventory against AGENTS.md, but spec-kit bootstrap doesn't auto-document its own additions. | spec 002, fleet-generated |
| L6 | Deterministic tests must not transitively invoke Copilot-backed optimizer phases. The readiness blocker was not a tier-selection bug but a test-contract bug: `make test` called `repo-optimizer.sh`, which entered discovery/critic/synthesis and stalled on `copilot`. Fixing it required an explicit `OPTIMIZER_PREFLIGHT_ONLY=true` mode so tests can validate budget-tier behavior while skipping phases 2-4. | 2026-03-14 readiness-blocker repair: `make test` hung in `tests/test-preflight-tiers.sh` until `repo-optimizer.sh` exposed a pre-flight-only path; after wiring tests to that mode, `make test` and `make check` both passed. |
| L7 | Pattern B equivalence needs two retained facts: the launched root command and the final terminal bundle state. On the 2026-04-13 bound replay for the `v423` owner-side question on the `v414` proof line, Copilot launched the exact `repo-optimizer.sh` command but cancelled during shell-output wait before closeout, leaving only pre-flight/discovery artifacts. | 2026-04-13 bounded Pattern B replay for `v423` / `v414`, `work/20260413T223123Z` |
| L8 | When Copilot-backed phases write raw JSONL to disk instead of streaming their own output, the orchestrator still needs bounded stdout progress or the public Pattern B wrapper can die at the shell-supervision layer before terminal receipts are written. Emitting phase progress during each long Copilot call was enough for the 2026-04-13 bound replay to stay alive through critic, synthesis, and `RUNTIME_RECEIPTS.json` closeout. | 2026-04-13 Pattern B compatibility repair, `work/20260413T225957Z` |
| L9 | The shell-output blocker on the retained `v414` proof seam is the long silent window between phase-boundary outputs. Stretching `OPTIMIZER_PROGRESS_INTERVAL=500` seconds recreated the Pattern B cancellation and left only pre-flight plus partial discovery artifacts. | 2026-04-14 owner-side causal isolation replay, `work/20260414T013625Z` |
| L10 | On the actual older bound `repo-optimizer@12f2b2a334337f9e86de2784a4eed57e548a3eae` surface, replaying the same public Pattern B command after applying only the script-level bounded-stdout heartbeat delta was enough to flip the result from pre-flight-plus-discovery loss to a full terminal bundle with completed critic, completed synthesis, and retained `RUNTIME_RECEIPTS.json`. That closes the owner-side equivalence gap more directly than comparing only the later repaired surface to the older failure receipt. | 2026-04-14 older-bound heartbeat equivalence replay, `work/20260414T123522Z` |
