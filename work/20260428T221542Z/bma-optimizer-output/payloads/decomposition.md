| Rank | Severity | Finding | File | Token Impact | Evidence Quote | Verification |
|---:|---|---|---|---|---|---|
| 1 | HIGH | Monolithic bundle builder with 1611 lines handling 11+ distinct phases including corpus freeze, intent analysis, principle ledger parsing, window analysis, and 4-phase narrative assembly | scripts/build-principle-realignment-bundle.py | ~50K | `WINDOW_LABELS = ["03-07..03-16", "03-17..03-25", "03-26..03-27", "03-28..03-29", "03-30..04-02", "04-03..04-06"]` + `PRINCIPLE_STATUS_MAP` + multiple subprocess calls | `wc -l scripts/build-principle-realignment-bundle.py` |
| 2 | HIGH | Token cost analysis bundle builder with 1913 lines merging session parsing, pricing snapshots, coverage audits, history analysis, and 2-tier report generation | scripts/build-newsletter-token-cost-analysis.py | ~60K | `DEFAULT_NEWSLETTER_REPO` + `AUDIT = import_script_module("newsletter_token_audit"` + `HISTORY = import_script_module("newsletter_usage_history"` | `wc -l scripts/build-newsletter-token-cost-analysis.py` |
| 3 | HIGH | Make target clock-time analyzer with 1639 lines handling command pattern matching, architecture row definitions, treatment suggestions, and TSV output generation | scripts/analyze-make-clock-time.py | ~52K | `ARCHITECTURE_ROWS` dict with 7 targets + `TREATMENTS` dict + `MAKE_COMMAND_PATTERN` regex + classification logic | `wc -l scripts/analyze-make-clock-time.py` |
| 4 | HIGH | GPT-5.5 runtime regression analyzer with 1345 lines combining session discovery, workspace classification, model-time scanning, and phased analysis | scripts/analyze-gpt55-runtime-regression.py | ~42K | `def discover_records(repos: set[str])` + codex/vscode/copilot-cli session parsing + `def canonical_workspace(meta, repos)` | `wc -l scripts/analyze-gpt55-runtime-regression.py` |
| 5 | HIGH | Token usage analyzer with 1432 lines merging session parsing, model surface inventory, hotspot detection, and replacement hypothesis evaluation | scripts/analyze-token-usage.py | ~45K | `REPLACEMENT_HYPOTHESES = [ReplacementHypothesis(hypothesis_id="H1"` + `CANONICAL_COMPONENTS` + `REMOTE_TOKEN_RE` + multiple RE patterns | `wc -l scripts/analyze-token-usage.py` |
| 6 | HIGH | Phase fingerprint analyzer with 1093 lines handling CLI parsing, phase boundary definitions, tool normalization, strategy classification, and report generation | scripts/analyze-phase-fingerprints.py | ~35K | `ACTION_NAMES`, `READ_NAMES`, `DELEGATE_NAMES` + `STRATEGY_LABELS = ["action-dominant", "read-dominant", "edit-dominant", "bash-dominant", "delegate-and-assemble", "mixed", "no-tools"]` | `wc -l scripts/analyze-phase-fingerprints.py` |
| 7 | HIGH | Closeout disposition validator with 1062 lines handling work.md parsing, artifact contract validation, operator intent checks, finding extraction, and reconciliation status | scripts/closeout-disposition.py | ~33K | `SEVERITY_RE`, `MANDATORY_SEVERITIES`, `AUTO_DEFER_SEVERITIES`, `VALID_WORK_TYPES` + operator_intent_contract imports | `wc -l scripts/closeout-disposition.py` |
| 8 | MEDIUM | Session grader with 1003 lines implementing 4-dimension 15pt scoring across auditor, advisor, optimizer, and critic surface checks | scripts/score-session.sh | ~31K | `# Section 1: Auditor checks (8 pts)` + `# Section 2: Advisor checks (12 pts)` + `check() { local id="$1" category="$2" desc="$3" result="$4"` | `wc -l scripts/score-session.sh` |
| 9 | MEDIUM | Handoff-closeout sync validator with 927 lines handling handoff parsing, outcome conversion validation, reconciliation checks, and parallel lane state analysis | scripts/validate-handoff-closeout-sync.py | ~29K | `SECTION_RE`, `HEADING_ALIASES`, `def parse_parallel_lane_fields(work_dir)`, `def build_parallel_lane_state(work_dir, repo_root)` | `wc -l scripts/validate-handoff-closeout-sync.py` |
| 10 | MEDIUM | Stage 17 post-closure retrospective bundle builder with 835 lines handling 4-phase corpus freeze, intel bundle generation, principle analysis, and session manifest assembly | scripts/build-stage17-post-closure-operating-model-retro-bundle.py | ~26K | `PHASES` with A/B/C/D phase dicts + `KEY_CANONICAL_PATHS` list + `ACTIVE_REPOS`, `COMPARATOR_REPOS`, `EXCLUDED_REPOS` | `wc -l scripts/build-stage17-post-closure-operating-model-retro-bundle.py` |
| 11 | MEDIUM | Closeout reconciliation handler with 761 lines managing parallel lane state, work type parsing, git dirty paths, and run directory discovery | scripts/closeout-reconciliation.py | ~24K | `def parse_parallel_lane_fields(work_dir)`, `RUN_DIR_LINE_RE`, `def shared_surface_category_for_path(path)`, `PARALLEL_LANE_SECTION_RE` | `wc -l scripts/closeout-reconciliation.py` |
| 12 | MEDIUM | Fleet output scorer with 743 lines implementing 8+12pt scoring across auditor and advisor surface checks with Python/shell hybrid validation | scripts/score-fleet-output.sh | ~23K | `check "AU-1" "AUDITOR" "SCORECARD.json exists"` through `check "AD-8"` + extensive JSON validation via python3 -c snippets | `wc -l scripts/score-fleet-output.sh` |
| 13 | MEDIUM | External critique mismatch reconciler with 657 lines handling receipt/trace pairing, prompt SHA matching, coverage classification, and CSV ledger generation | scripts/reconcile-external-critique-mismatches.py | ~21K | `def pair_rows(receipts, traces, pair_kind, builder)` + `class ReceiptRow`, `class TraceRow`, `def classify_receipt_context` | `wc -l scripts/reconcile-external-critique-mismatches.py` |
| 14 | MEDIUM | External critique coverage freezer with 612 lines managing package discovery, receipt classification, trace matching, and root package alignment checks | scripts/freeze-external-critique-coverage.py | ~19K | `def classify_root_package(receipt_present, receipt_counts_as_success, closeout_present, closeout_ran, ser_present...)` + nested receipt scanning | `wc -l scripts/freeze-external-critique-coverage.py` |
| 15 | MEDIUM | speckit.checklist agent with 294 lines handling dynamic clarifying questions, context loading, and checklist generation using "unit tests for requirements" metaphor | targets/T6-repo-upgrade-advisor-B4/.github/agents/speckit.checklist.agent.md | ~9K | `## Checklist Purpose: "Unit Tests for English"` + `Generation algorithm:` with 6-step signal extraction | `wc -l targets/T6-repo-upgrade-advisor-B4/.github/agents/speckit.checklist.agent.md` |
| 16 | MEDIUM | speckit.specify agent with 258 lines managing feature branch creation, spec template filling, and progressive clarification workflow with 5+ execution steps | targets/T6-repo-upgrade-advisor-B4/.github/agents/speckit.specify.agent.md | ~8K | `2. **Check for existing branches before creating new one**:` + fetch/grep/number logic + `4. Follow this execution flow:` 8-step cascade | `wc -l targets/T6-repo-upgrade-advisor-B4/.github/agents/speckit.specify.agent.md` |
| 17 | MEDIUM | Extract repo DNA script with 236 lines handling git analysis, skill counting, agent parsing, and snapshot generation with mixed shell/Python logic | scripts/extract-repo-dna.sh | ~7K | `# Phase 1: Extract commit metadata` + `# Phase 2: Count agents and skills` + Python pandas integration for snapshot | `wc -l scripts/extract-repo-dna.sh` |
| 18 | LOW | Spec orchestrator with 210 lines managing 9-stage spec-kit pipeline invocation with agent dispatch and state management | scripts/spec-orchestrator.sh | ~6.5K | `STAGES=("specify" "clarify" "plan" "tasks" "analyze" "implement" "checklist" "constitution" "taskstoissues")` + runSubagent calls | `wc -l scripts/spec-orchestrator.sh` |
